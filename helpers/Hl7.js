/* const resyu  = req.body.Lines.map(function(obj){
    let  rObj = {};
    rObj[obj.PRODUCTOID] = obj.PRODUCTONAME;
    return rObj;

    
 
}); */


  //const filename = `${CODE}-${year}${month}${day}-${hour}${minutes}-${seconds}-${mili}.txt`
  //const filename = `${CODE}.txt`
               
  //    //-------------------si funcionaa
  //const id = req.body.Lines.map(({ PRODUCTOID }) => PRODUCTOID).join("|");
  //const name = req.body.Lines.map(({ PRODUCTONAME ,i}) => PRODUCTONAME).join("|");
  //console.log(id)
  /* const data =`MSH|^~\&||${AREA}||${CODE}||${DATE}||${HOUR}||
  OBR|||| ${resyu}`

  fs.writeFileSync( `${filename}`,data)   */